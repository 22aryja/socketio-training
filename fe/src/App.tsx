import Chat from "./components/Chat";

const App = () => {
    return (
        <main className="w-screen h-screen flex justify-center items-center">
            <Chat />
        </main>
    );
};

export default App;
